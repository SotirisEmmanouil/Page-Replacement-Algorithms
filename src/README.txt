To run Page.java, simply place the file in any IDE (with Java 8 installed on the machine) 
and run according to how you would run another program. 
You can also run it on terminal (assuming Java 8 is installed on your machine)
with the commands javac Page.java, then java Page. 